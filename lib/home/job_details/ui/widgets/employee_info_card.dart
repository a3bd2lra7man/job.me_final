import 'package:flutter/material.dart';

class EmployeeInfoCard extends StatelessWidget {
  const EmployeeInfoCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
